import React from 'react';
import LoginForm from '../Components/organisms/AccountForm/LoginForm';

interface LoginProps {
    
}

const LoginPage: React.FC = () => {
    return (
        <div>
            <LoginForm/>
        </div>
    );
};

export default LoginPage;